/*
export class PoidsController {
    static getAllPoids = (req, res) => {
        return res.status(202).send({
            // error: err.message || `Unable to find user with id ${data.id}`,
            error: "Poids en cours de développement",
            code: "UL1",
        });
    };
}
*/
